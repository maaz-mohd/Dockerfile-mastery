# Day 1 — Foundations

**Goal:** leave today able to explain image/container/layer from memory, and
know every core Dockerfile instruction — especially `CMD` vs `ENTRYPOINT` — cold.

1. [`01-mental-model.md`](./01-mental-model.md) — image vs container vs layer, the core CLI loop
2. [`02-core-instructions.md`](./02-core-instructions.md) — `FROM`, `RUN`, `WORKDIR`, `COPY`/`ADD`, `ENV`/`ARG`, `EXPOSE`, `CMD` vs `ENTRYPOINT`, `LABEL`/`USER`/`VOLUME`

End of day: [`checklist.md`](./checklist.md)

No hands-on builds today — that starts Day 2. Today is concepts, done properly.
