# Day 4 — Multi-Stage Builds II (React, Java) + BuildKit

**Goal:** finish the language tour with a frontend SPA and an enterprise JVM
app, then level up your builds with BuildKit's cache mounts and multi-platform
support.

1. [`01-react/`](./01-react/) — build stage + nginx runtime for a static SPA
2. [`02-java-spring/`](./02-java-spring/) — Maven build stage + JRE-only runtime
3. [`03-buildkit-features.md`](./03-buildkit-features.md) — cache mounts, `DOCKER_BUILDKIT=1`

End of day: [`checklist.md`](./checklist.md)
